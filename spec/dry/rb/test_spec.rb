require "spec_helper"

describe Dry::Rb::Test do
  it "has a version number" do
    expect(Dry::Rb::Test::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
